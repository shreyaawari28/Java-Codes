
# Java Palindrome Number Check – Example Program

This repository contains a Java program that demonstrates how to **check whether a number is a palindrome** using basic arithmetic operations.

The program is intended for **beginners** to understand loops, conditionals, and number reversal logic.

---

## 📌 Program Overview

The program checks if an integer remains the same when its digits are **reversed**.  
If the reversed number matches the original number, it is considered a **palindrome**.

---

## 🧪 Code Functionality

- Declares an integer variable `digit`
- Stores the original value for comparison
- Handles negative number input using a conditional check
- Uses a `while` loop to:
  - Extract the last digit using modulus (`%`)
  - Build the reversed number
  - Reduce the original number using division (`/`)
- Compares the reversed number with the original number
- Prints whether the number is a palindrome or not

---

## 🧠 Concepts Covered

- Palindrome logic  
- Integer arithmetic  
- `while` loop  
- Conditional statements (`if-else`)  
- Modulus (`%`) and division (`/`) operators  
- Variable storage and comparison  
- Console output using `System.out.println()`  

---

## 🖥️ Output

📸 **Console output showing palindrome check result:**  
![Program Output](output.png)

---

## 📂 File Information

- `palindrome.java` — Java source code  
- `output.png` — Screenshot of the program output  
- `README.md` — Project documentation  

---

## ⚠️ Limitations

- Number is hardcoded
- No user input support
- Does not handle very large integers
- Negative numbers are only checked for validity, not processed

---

## 👨‍💻 Author

**Shreya Awari**  
📧 Email: shreyaawari31@gmail.com  
🌐 GitHub: https://github.com/shreyaawari28  

---

⭐ Star the repository if it helps you understand palindrome logic in Java.
